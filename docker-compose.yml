version: '3.8'

services:
  antiimpulse_bot:
    build: .
    container_name: antiimpulse_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/bot.db
